# Nvim 12 Lightconfig Loss

## Plugins

- `nvim-treesitter` - syntaxe highlighting
- `mini-test` - test
- `mini-pick` - find files
- `mason` - lsp installer
- `nvim-lspconfig`

## Details

- Need a C complier `cc` command for lsp and treesitter.
- Need `tree-sitter-cli` 0.26.1 or later installed via package manager, not npm.
